//
//  BannerContent.swift
//  inStudio
//
//  Created by Peter.Hsu on 2016/11/23.
//  Copyright © 2016年 Peter.Hsu. All rights reserved.
//

import UIKit
import AVFoundation

class BannerContent: UIViewController {

    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    
    
    @IBAction func playBtnTapped(_ sender: UIButton) {
        /*var player:AVPlayer!
        var playerItem:AVPlayerItem!;
        var avPlayerLayer:AVPlayerLayer = AVPlayerLayer(player: player)
        avPlayerLayer.frame = CGRectMake(view.frame)
        self.view.layer .addSublayer(avPlayerLayer)
        var steamingURL:NSURL = NSURL(string:playerURL)
        player = AVPlayer(URL: steamingURL)
        player.play()*/
    }

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destinationViewController.
        // Pass the selected object to the new view controller.
    }
    */

}
